    <a href="/post/{{id}}">
    <article class="post card">
      <div class="title card-header">
        <h1>{{title}}</h1>
        <h2>Posted by {{user.username}} on
        {{format_date created_at}}</h2>
      </div>
      <div class="meta card-body">
        
        <div class="text">
      {{contents}}
    </div>
        {{comments.length}} comment(s)
      </div>
    </article>
    </a>